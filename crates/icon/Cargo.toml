[package]
name = "strudel-icon"
version.workspace = true
edition = "2024"
description = "Composite macOS-style squircle app icons from foreground artwork"
repository = "https://github.com/octavore/strudel"
license = "Apache-2.0"

[dependencies]
image = {version = "0.25", default-features = false, features = ["png"]}
resvg = "0.47.0"
thiserror = "2"
tiny-skia = "0.12"

[dev-dependencies]
tempfile = "3"
